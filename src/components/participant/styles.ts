import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        width: "100%",
        backgroundColor: "#1F1E25",
        alignItems: "center",
        borderRadius: 5,
        marginBottom: 10
    },
    name: {
        flex: 1,
        fontSize: 16,
        color: "#FFF",
        padding: 16
    }

});